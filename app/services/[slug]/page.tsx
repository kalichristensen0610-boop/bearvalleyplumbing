import type { Metadata } from "next"; import Link from "../../../components/ui/HardLink"; import { notFound } from "next/navigation"; import { serviceBySlug,services } from "../../../data/services"; import { Breadcrumbs,CTA,FAQList,PageHero,SectionHead,ServiceGrid } from "../../../components/ui/SiteComponents";
export function generateStaticParams(){return services.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,s=serviceBySlug(slug);if(!s)return{};return{title:s.metaTitle.replace(" | Bear River Plumbing",""),description:s.metaDescription,alternates:{canonical:`/services/${s.slug}`},openGraph:{title:s.metaTitle,description:s.metaDescription,images:[]},twitter:{title:s.metaTitle,description:s.metaDescription,images:[]}}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,s=serviceBySlug(slug);if(!s)notFound();const related=services.filter(x=>s.related.includes(x.slug));const schema={"@context":"https://schema.org","@type":"Service",name:s.name,description:s.summary,provider:{"@type":"Plumber",name:"Bear River Plumbing LLC"},areaServed:["Eastern Idaho","Treasure Valley, Idaho"]};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><div className="content-width"><Breadcrumbs items={[{label:"Home",href:"/"},{label:"Services",href:"/services"},{label:s.name}]}/></div><PageHero eyebrow={s.category} title={s.hero} lead={s.summary}/><section className="section service-detail"><div><SectionHead eyebrow="How we can help" title={`${s.name}, handled with a clear plan.`}/><p>Every property and plumbing system is different. Bear River Plumbing starts by understanding what you are seeing, where the property is located, and what outcome you need.</p><ul className="check-list">{s.details.map(x=><li key={x}>{x}</li>)}</ul></div><aside><h2>Request this service</h2><p>Share a few details and our team will confirm service-area coverage and follow up.</p><Link className="btn" href={`/request-service?service=${s.slug}`} data-cta="service-page-request">Request {s.name}</Link></aside></section><section className="section muted"><SectionHead eyebrow="Related plumbing" title="You may also need"/><ServiceGrid items={related}/></section><section className="section"><SectionHead eyebrow="Questions" title={`About ${s.name.toLowerCase()}`}/><FAQList items={s.faqs}/></section><CTA id="service-final-request"/></main>}
