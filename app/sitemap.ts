import type { MetadataRoute } from "next"; import { services } from "../data/services"; import { locations } from "../data/locations";
export default function sitemap():MetadataRoute.Sitemap{const base="https://rosybrown-armadillo-385824.hostingersite.com",now=new Date();const staticRoutes=["","/about","/contact","/request-service","/residential-plumbing","/services","/service-areas"];return [...staticRoutes.map(path=>({url:`${base}${path}`,lastModified:now,changeFrequency:path===""?"weekly" as const:"monthly" as const,priority:path===""?1:.8})),...services.map(s=>({url:`${base}/services/${s.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.75})),...locations.map(l=>({url:`${base}/service-areas/${l.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.8})),...locations.flatMap(l=>services.map(s=>({url:`${base}/service-areas/${l.slug}/${s.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.7})))]}
