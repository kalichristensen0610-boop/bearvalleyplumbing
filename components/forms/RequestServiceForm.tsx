"use client";
import { useState } from "react"; import { services } from "../../data/services";
export function RequestServiceForm({origin="request-service"}:{origin?:string}){const [status,setStatus]=useState<"idle"|"sending"|"sent"|"error">("idle");const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus("sending");setMessage("");const form=new FormData(e.currentTarget);const payload=Object.fromEntries(form.entries());try{const res=await fetch("/api/request-service",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...payload,origin,ctaId:"request-form-submit"})});const body=await res.json() as {message?:string};if(!res.ok)throw new Error(body.message||"Unable to send your request.");setStatus("sent");setMessage(body.message||"Thanks. Your request has been sent.");e.currentTarget.reset()}catch(error){setStatus("error");setMessage(error instanceof Error?error.message:"Unable to send your request.")}}
return <form className="request-form" onSubmit={submit} aria-describedby="form-note" noValidate><div className="form-grid"><label>Name*<input name="name" autoComplete="name" required/></label><label>Phone*<input name="phone" type="tel" autoComplete="tel" required/></label><label>Email<input name="email" type="email" autoComplete="email"/></label><label>Service address<input name="address" autoComplete="street-address"/></label><label>City*<input name="city" autoComplete="address-level2" required/></label><label>Property type*<select name="customerType" required defaultValue=""><option value="" disabled>Select one</option><option>Residential</option><option>Commercial</option></select></label><label>Service needed*<select name="service" required defaultValue=""><option value="" disabled>Select a service</option>{services.map(s=><option key={s.slug} value={s.name}>{s.name}</option>)}<option>Not sure</option></select></label><label>Preferred contact<select name="preferredContact" defaultValue="Phone"><option>Phone</option><option>Email</option><option>Text</option></select></label><label className="full">Tell us about the problem or project*<textarea name="description" rows={5} required/></label></div><input className="honeypot" name="companyWebsite" tabIndex={-1} autoComplete="off" aria-hidden="true"/><label className="consent"><input type="checkbox" name="consent" value="yes" required/> I agree that Bear River Plumbing may contact me about this request.*</label><p id="form-note" className="form-note">Submitting this form requests contact and does not confirm an appointment. Do not use this form for an active emergency.</p><button className="btn" type="submit" disabled={status==="sending"}>{status==="sending"?"Sending…":"Send Service Request"}</button>{message&&<p className={`form-status ${status}`} role="status">{message}</p>}</form>}
